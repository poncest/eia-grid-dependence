# Data Dictionary

This dictionary is intentionally selective: source data, controlled vocabularies, the tables that carry the analysis, and the final chart tables. The other files in `data/` and `output/` are intermediate validation artifacts generated by the pipeline and are not documented column by column.

## 1. Source data (EIA Open Data API, Form EIA-930)

Cached in `data-raw/` as parquet after `janitor::clean_names()`, with `period` parsed as UTC and `value` as numeric (`analysis/01`). The pull helper is `R/eia_api.R`.

**Interchange (BA-to-BA), hourly**

| Field | Meaning |
|---|---|
| `period` | Hour of the observation (UTC) |
| `fromba`, `toba` | Balancing authority codes on each side of the reported exchange |
| `value` | Reported interchange, in megawatthours (MWh) |

_TODO: confirm any additional cached columns (such as names and units) against the headers in `data-raw/`._

**Demand, hourly:** `period` (UTC hour), the BA or region code, and `value` (demand in MWh; `US48` is the Lower 48 total). _TODO: confirm cached column names against `data-raw/demand_us48_2025.parquet` and the monthly node-demand files._

## 2. Conventions and controlled vocabularies

**Canonical pair ordering and direction.** Each connection is stored once as an alphabetically ordered pair (`ba_1` < `ba_2`, built with `pmin`/`pmax` of the reported codes). In the reconciled tables a signed value **≥ 0 means flow from `ba_1` to `ba_2`**; script 09 counts that flow as outgoing for `ba_1` and incoming for `ba_2`, and the reverse when negative. _TODO: add one sentence on the raw EIA sign convention from script 06 or `R/eia_api.R`._

| Vocabulary | Values | Meaning |
|---|---|---|
| Entity class (05) | `balancing_authority`, `regional_aggregate`, `national_aggregate` | Classification of all 86 nodes |
| Connection layer (06) | `ba_to_ba`, `region_to_region` | Separate networks, never combined |
| Reconciliation scenario | `lower`, `mid`, `upper` | How disagreeing reciprocal reports are resolved |
| Demand coverage (08) | `COMPLETE`, `NEAR_COMPLETE`, `NO_DEMAND_SERIES` | Node demand availability; the first two are usable |
| Flow status (09) | `FULLY_OBSERVED`, `PARTIALLY_CONFLICT_EXCLUDED`, `FULLY_CONFLICT_EXCLUDED` | Whether a node-hour's connections all had a resolved direction. Primary results use only `FULLY_OBSERVED`. |
| Claim status (09) | `PRIMARY`, `WARNING`, `EXCLUDE` | Data-quality gate: `PRIMARY` needs retained stress weight ≥ 0.95, positive-demand coverage ≥ 0.95 and resolved-flow coverage ≥ 0.90; `WARNING` allows retained weight down to 0.80; otherwise `EXCLUDE` |
| Measure (09) | `gross_import_ratio`, `raw_net_import_ratio`, `net_import_dependence`, `net_export_intensity` | See section 3 |
| Stress threshold (10) | 1%, 5% (baseline), 10% | Share of highest-demand US48 hours tagged as stress |
| Candidate role (10) | `core_dependence`, `core_direction_flip`, `coverage_sensitivity`, `minor_confirmed_flip`, `regional_context` | Why each node is in the 8-node registry |
| Overall status (10) | `ROBUST`, `MINOR`, `THRESHOLD_SENSITIVE`, `COVERAGE_SENSITIVE`, `NOT_ROBUST` | One verdict per candidate; see the rule below |
| Robustness flags (10) | `EVENT_VARIABLE`, `NEAR_COVERAGE_GATE` | Informational; do not change `overall_status` |

## 3. Measures

All are computed as **ratios of sums** within each season × hour-of-day (UTC) stratum, separately for stress and normal hours, then combined across strata with the fixed stratum weights from script 07 (only strata that contain both stress and normal hours are used).

| Measure | Definition |
|---|---|
| `gross_import_ratio` | `sum(incoming MWh) ÷ sum(demand MWh)` |
| `raw_net_import_ratio` | `(sum(incoming) − sum(outgoing)) ÷ sum(demand)`. Signed: positive = net importer, negative = net exporter. Used to detect exporter-to-importer flips. |
| `net_import_dependence` | `max(raw_net_import_ratio, 0)`. Primary "how import-dependent" measure. |
| `net_export_intensity` | `max(−raw_net_import_ratio, 0)`. Export-side mirror. |

Ratios are proportions; the chart reports differences in percentage points (×100). Hours with zero, negative or missing demand are never used in a denominator.

**Verdict rule (script 10), first failing condition wins.** A claim "holds" for dependence candidates when the stress-minus-normal difference is positive, and for flip candidates only when the normal value is ≤ 0 and the stress value is > 0.

1. `NOT_ROBUST`: the baseline (mid scenario, `PRIMARY` claim status) does not hold, or the claim fails in any of the three scenarios.
2. `THRESHOLD_SENSITIVE`: the claim fails at any of the 1%, 5% or 10% thresholds.
3. `COVERAGE_SENSITIVE`: the stress-minus-normal difference changes sign when partially observed flows are included.
4. `MINOR`: passes everything above but is registered as a minor confirmed flip (PNM, PSCO).
5. `ROBUST`: everything else.

Flags: `EVENT_VARIABLE` if the claim holds in fewer than 2 of the 3 named event windows; `NEAR_COVERAGE_GATE` if flow or demand coverage is within 3 percentage points of its gate.

## 4. Key analytical tables

| Table | Produced by | One row is... | Key columns |
|---|---|---|---|
| `pair_hour_estimates_full` | 06 | A BA pair (connection) in one hour | `connection_layer`, `ba_1`, `ba_2`, `period`, `lower_signed`, `mid_signed`, `upper_signed` (MWh, sign per section 2), `direction_conflict` (the two reports disagree on direction) |
| `node_demand_coverage` | 08 | A node | `code`, `name`, `entity_type`, `coverage_status`, `exclusion_reason` |
| `node_hourly_flow_ba` (and `_region`) | 09 | A node-hour | `node`, `period`, `total_connections_active`, `conflicted_connections`, `resolved_connections`, `resolved_connection_pct`, `flow_status`, and `incoming_{lower,mid,upper}_mwh` / `outgoing_{lower,mid,upper}_mwh`. Demand, season, hour-of-day and stress tags are joined in memory in script 09 and are not saved in this file. |
| `dependence_stratified_ba` (and `_region`) | 09 | A node × measure × scenario | `weighted_stress_value`, `weighted_normal_value`, `weighted_difference`, `retained_stress_weight`, `stress_observations`, `normal_observations`, `resolved_flow_coverage`, `positive_demand_coverage`, `claim_status` |
| `robustness_*.csv` (five files) | 10 | A candidate under one test | Threshold sensitivity, named-event sensitivity, scenario survival, flow completeness and regional coherence, one file per axis |
| `candidate_verdicts.csv` | 10 | A candidate | `node`, `candidate_role`, `primary_measure`, `baseline_holds`, `scenario_ok`, `threshold_ok`, `flow_ok`, `event_ok`, `near_coverage_gate`, `overall_status`, `robustness_flags` |
| `corridor_attribution_*.csv` | 11 | A corridor (neighbor) of a validated node | _TODO: columns from script 11_ |

## 5. Chart tables (`analysis/12`, `analysis/13`)

Arc tables drawn on the final chart (`nw_arcs`, `central_arcs`):

| Column | Meaning |
|---|---|
| `target` | The BA whose dependence changed (e.g., TPWR, SCL, AECI) |
| `counterparty` | The neighboring BA on the other end of the corridor |
| `contribution_pp` | That corridor's contribution to the stress-vs-normal change, in percentage points. Positive values increase dependence. |
| `direction` | `Driver (increases dependence/flip)` or `Countervailing (offsets it)`; drives color |
| `lon_from`, `lat_from`, `lon_to`, `lat_to` | Schematic endpoints (BA anchor points), not transmission routes |

Anchor table: `code` (BA code), `lon`, `lat`. Anchors are relationship-specific placements derived from the Esri Policy Maps layer (vintage unconfirmed) and are for orientation only.
